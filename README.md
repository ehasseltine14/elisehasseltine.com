## elisehasseltine.com

Personal site for Elise Hasseltine. Hosted on GitHub Pages with the custom domain `elisehasseltine.com`.

The page is a single-file React prototype: `index.html` loads React + Babel from CDN and the JSX files (`app.jsx`, `tweaks-panel.jsx`) compile in the browser.
